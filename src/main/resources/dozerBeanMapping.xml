<?xml version="1.0" encoding="UTF-8"?>
<mappings xmlns="http://dozermapper.github.io/schema/bean-mapping"
          xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
          xsi:schemaLocation="http://dozermapper.github.io/schema/bean-mapping
          http://dozermapper.github.io/schema/bean-mapping.xsd">
    <configuration>
        <!-- 默认是 true，当发生转换错误时抛出异常，停止转换，这里设置成false，如果转换错误，继续转换 -->
        <stop-on-errors>false</stop-on-errors>
        <date-format>yyyy-MM-dd HH:mm:ss</date-format>
        <custom-converters>
            <converter type="com.zimug.bootlaunch.utils.LocalDateTimeToDateDozerConverter">
                <class-a>java.time.LocalDateTime</class-a>
                <class-b>java.util.Date</class-b>
            </converter>
        </custom-converters>
    </configuration>
    <!--<mapping date-format="MM-dd-yyyy HH:mm:ss">
        <class-a>com.zimug.bootlaunch.pojo.ArticleVO</class-a>
        <class-b>com.zimug.bootlaunch.generator.Article</class-b>
        <field>
            <a>createTime</a>
            <b>createtime</b>
        </field>
    </mapping>-->
</mappings>